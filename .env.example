NODE_ID=optiplex_test
SELF_NODE_ID=optiplex_test
NODE_IP=
MESH_JOIN_TOKEN=
# REQUIRED for the primary/genesis node (e.g. optiplex_main). Generate a random string.
# Leave blank ONLY if joining an existing mesh via MESH_JOIN_TOKEN.
JWT_SECRET=
ADMIN_USERNAME=admin2
ADMIN_EMAIL=admin2@y.com
ADMIN_PASSWORD=password
DB_PATH=/data/registry.db
STORAGE_ROOTS=